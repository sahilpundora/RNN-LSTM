INTRODUCTION
Could we possibly predict future movements of, say, AAPL Stocks, by analyzing the last 10 days of closing price and volume of the stock? To check, if there exists some, at least better
than random, relationship here that a recurrent neural network could discover.
